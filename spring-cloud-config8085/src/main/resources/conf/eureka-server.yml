eureka: #配置Eureka服务端相关配置
  instance:
    instance-id: ${spring.application.name}:${server.port} # Eureka服务端的名称
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:7999/eureka/ # Eureka服务端的注册中心